import{_ as a}from"./plugin-vue_export-helper-c27b6911.js";import{o as s,c as e,b as n,f as t}from"./app-36705845.js";const o={},d=t(`<h1 id="值编号-value-numbering" tabindex="-1"><a class="header-anchor" href="#值编号-value-numbering" aria-hidden="true">#</a> 值编号（Value Numbering）</h1><p><strong>值编号</strong>是一种简单的表达式优化技术，它通过在程序中识别出相同的表达式，然后将它们替换为同一个临时变量来消除冗余的计算。</p><p>这种技术的基本思想是，如果两个表达式的值相同，那么它们就可以用同一个临时变量来表示。例如，对于表达式<code>a+b</code>和<code>c+d</code>，如果<code>a=c</code>且<code>b=d</code>，那么这两个表达式的值就相同，可以用同一个临时变量来表示。这样我们通过对每个表达式都进行标号，就可以追踪每个表达式的值，从而消除冗余的计算。</p><h2 id="值编号的基本做法" tabindex="-1"><a class="header-anchor" href="#值编号的基本做法" aria-hidden="true">#</a> 值编号的基本做法</h2><p>值编号技术主要基于以下两个核心原理：</p><ol><li><p><strong>表达式等价性</strong></p><ul><li>如果两个表达式在语义上等价，那么它们会产生相同的值</li><li>等价性判断需要考虑操作符的交换律、结合律等代数性质</li><li>例如：<code>a+b</code> 和 <code>b+a</code> 是等价的</li></ul></li><li><p><strong>值映射表</strong></p><ul><li>维护一个映射表，记录每个表达式的值编号</li><li>对于每个新表达式，先查找是否已存在等价表达式</li><li>如果存在，使用已有的值编号</li><li>如果不存在，分配新的值编号</li></ul></li></ol><h2 id="一个直观的例子" tabindex="-1"><a class="header-anchor" href="#一个直观的例子" aria-hidden="true">#</a> 一个直观的例子</h2><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code><span class="token keyword">int</span> <span class="token function">compute</span><span class="token punctuation">(</span><span class="token keyword">int</span> a<span class="token punctuation">,</span> <span class="token keyword">int</span> b<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">int</span> c <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
    <span class="token keyword">int</span> f <span class="token operator">=</span> b<span class="token punctuation">;</span>
    <span class="token keyword">int</span> d <span class="token operator">=</span> c <span class="token operator">*</span> a<span class="token punctuation">;</span>
    <span class="token keyword">int</span> b <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
    <span class="token keyword">int</span> e <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
    <span class="token keyword">int</span> g <span class="token operator">=</span> f <span class="token operator">+</span> a<span class="token punctuation">;</span>
    <span class="token keyword">int</span> h <span class="token operator">=</span> a <span class="token operator">*</span> g<span class="token punctuation">;</span>
    <span class="token keyword">return</span> e <span class="token operator">-</span> h<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>我们仔细观察可以发现，表达式<code>a+b</code>出现了两次，但实际上计算的并不是同一个值，因为<code>b</code>的值在第二次计算时被修改了。 而因为<code>f</code>等于<code>b</code>，所以<code>f+a</code>等于<code>b+a</code>，所以<code>a*g</code>等于<code>a*(b+a)</code>，与之前的<code>c*a</code>为同一计算。</p><p>值编号技术就是为了将相同&quot;值&quot;的表达式用同一个临时变量来表示，从而避开符号相同但值不同的表达式。</p><p>让我们为每个表达式分配一个值编号：</p><table><thead><tr><th>语句</th><th>被赋值符号</th><th>表达式</th><th>值编号</th><th>说明</th></tr></thead><tbody><tr><td><code>c = a + b</code></td><td>c: v3</td><td><code>a + b</code></td><td>v1 + v2</td><td><code>a</code>为v1，<code>b</code>为v2</td></tr><tr><td><code>f = b</code></td><td>f: v2</td><td><code>b</code></td><td>v2</td><td><code>b</code>仍为v2</td></tr><tr><td><code>d = c * a</code></td><td>d: v4</td><td><code>c * a</code></td><td>v3 * v1</td><td><code>c</code>为v3，<code>a</code>为v1</td></tr><tr><td><code>b = 1</code></td><td>b: v5</td><td><code>1</code></td><td>v5</td><td>常量1</td></tr><tr><td><code>e = a + b</code></td><td>e: v6</td><td><code>a + b</code></td><td>v1 + v5</td><td><code>a</code>仍为v1，新的<code>b</code>为v5</td></tr><tr><td><code>g = f + a</code></td><td>g: v3</td><td><code>f + a</code></td><td>v2 + v1</td><td><code>f</code>为v2，<code>a</code>为v1，发现 v1 + v2 已经计算过</td></tr><tr><td><code>h = a * g</code></td><td>h: v4</td><td><code>a * g</code></td><td>v1 * v3</td><td><code>a</code>为v1，<code>g</code>为v3，发现 v3 * v1 已经计算过</td></tr><tr><td><code>return e - h</code></td><td>-</td><td><code>e - h</code></td><td>v6 - v4</td><td><code>e</code>为v6，<code>h</code>为v8</td></tr></tbody></table><p>通过这个表格，我们可以清楚地看到：</p><ol><li>每个基本变量（如<code>a</code>、<code>b</code>）都有自己的值编号</li><li>复合表达式（如<code>a+b</code>）的值编号由其组成部分的值编号组成</li><li>当变量被重新赋值时（如<code>b = 1</code>），它获得新的值编号</li><li>相同的表达式组合会产生相同的值编号表示</li><li>每个被赋值的符号都获得一个新的值编号，除非它直接使用另一个变量的值编号</li></ol><p>所以化简后的代码为：</p><div class="language-c line-numbers-mode" data-ext="c"><pre class="language-c"><code><span class="token keyword">int</span> <span class="token function">compute</span><span class="token punctuation">(</span><span class="token keyword">int</span> a<span class="token punctuation">,</span> <span class="token keyword">int</span> b<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">int</span> c <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
    <span class="token keyword">int</span> f <span class="token operator">=</span> b<span class="token punctuation">;</span>
    <span class="token keyword">int</span> d <span class="token operator">=</span> c <span class="token operator">*</span> a<span class="token punctuation">;</span>
    <span class="token keyword">int</span> b <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
    <span class="token keyword">int</span> e <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
    <span class="token keyword">int</span> g <span class="token operator">=</span> c<span class="token punctuation">;</span>  <span class="token comment">// 发现 c 已经计算过，直接使用</span>
    <span class="token keyword">int</span> h <span class="token operator">=</span> d<span class="token punctuation">;</span>  <span class="token comment">// 发现 d 已经计算过，直接使用</span>
    <span class="token keyword">return</span> e <span class="token operator">-</span> d<span class="token punctuation">;</span> <span class="token comment">// 之后 g 和 h 其实可以被死代码删除清理掉</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,16);function c(p,l){return s(),e("div",null,[d,n(` 下面来试一试：
<ClientOnly><ValueNumbering /></ClientOnly> `),n(` ### Extended Basic Block

### Region

### Global `)])}const u=a(o,[["render",c],["__file","(1)值编号.html.vue"]]);export{u as default};
